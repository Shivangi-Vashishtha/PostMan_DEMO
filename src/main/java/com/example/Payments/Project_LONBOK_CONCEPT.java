package com.example.Payments;

import com.fasterxml.jackson.annotation.JsonProperty;

import lombok.AllArgsConstructor;
import lombok.Getter;
import lombok.Setter;

public class Project_LONBOK_CONCEPT {
	
	
	//it builts with java
	//getters,setters,contructors are easy to use just by annotations
	//to avoid boilerplate code(manually writter getter ,setter,constructurs)
	
	
	/*
	 * @Getter  // no need to write getter and setter for amount field as with the help of annotation its done
	 * @Setter
	 * String amount
	 * @Getter
@Setter
@AllArgsConstructor
@NoArgsConstructor
	 * 
	 * 
	  */
	
	// if we want to use Getter and Setter with every field then we can define it outside the class(means applicable for all the fields present in that class)
	
	 

}
